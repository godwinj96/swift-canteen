# Supabase Postgres connection strings.
# Get these from your Supabase project: Project Settings -> Database -> Connection string
#   DATABASE_URL = "Transaction" pooler connection (port 6543, ?pgbouncer=true) - used at runtime
#   DIRECT_URL   = "Session"/direct connection (port 5432) - used by `prisma migrate`
DATABASE_URL="postgresql://postgres.your-project-ref:your-password@aws-0-region.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.your-project-ref:your-password@aws-0-region.pooler.supabase.com:5432/postgres"

AUTH_SECRET="replace-with-a-32+char-random-secret"

# Bachs (bachs.io) hosted checkout — sandbox key while testing (sk_sandbox_...)
BACHS_API_KEY="sk_sandbox_placeholder"
BACHS_WEBHOOK_SECRET="bachs_placeholder_webhook_secret"
BACHS_BASE_URL="https://sandbox-api.bachs.io"

# Supabase Storage (admin menu-item image uploads) — Project Settings -> API
SUPABASE_URL="https://your-project-ref.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="replace-with-service-role-key"

# SendLib (sendlib.samueltuoyo.com) transactional email — connect a Gmail/Workspace account and generate a key
SENDLIB_API_KEY="replace-with-sendlib-api-key"
SENDLIB_FROM_EMAIL="you@your-connected-gmail-or-workspace-domain.com"

APP_URL="http://localhost:3000"
